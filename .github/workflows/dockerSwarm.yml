name: Docker swarm
on:
  pull_request:
  workflow_dispatch:
jobs:
  runner-job:
    runs-on: ubuntu-latest
    services:
      swarm:
        name: Run docker swarm
        steps:
          - uses: actions/checkout@v2
          - uses: ./actions/selenium-grid-action
        ports:
          - 4444:4444
