name: Docker swarm
on:
  pull_request:
  workflow_dispatch:
jobs:
  swarm:
    name: Run docker swarm
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./actions/selenium-grid-action
    ports:
      - 4444:4444
